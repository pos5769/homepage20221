<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="egovframework.let.reply.service.impl.ReplyMapper">
	
	<insert id="insertReply" parameterType="egovframework.let.reply.service.ReplyVO">
		INSERT INTO reply
		(rep_content, rep_writer, rep_reg_date, rep_bbs_no)
		VALUES
		(#{repContent}, #{repWriter}, NOW(), #{repBbsNo})
	</insert>
	
	<select id="selectReplyList" parameterType="String" resultType="egovframework.let.reply.service.ReplyVO">
		SELECT rep_no, rep_content, rep_writer, rep_reg_date, rep_bbs_no
		FROM reply
		WHERE rep_bbs_no = #{repBbsNo}
		ORDER BY rep_reg_date DESC
	</select>
	
	<delete id="deleteReply" parameterType="egovframework.let.reply.service.ReplyVO">
		DELETE
		FROM reply
		WHERE rep_no = #{repNo} AND rep_writer = #{repWriter}
	</delete>
	
</mapper>